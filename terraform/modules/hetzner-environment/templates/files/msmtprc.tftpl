defaults
auth           on
tls            on
tls_starttls   on
logfile        /var/log/msmtp.log

account        default
host           ${smtp_host}
port           ${smtp_port}
from           ${server_info_mail_from}
user           ${smtp_user}
password       ${smtp_password}
