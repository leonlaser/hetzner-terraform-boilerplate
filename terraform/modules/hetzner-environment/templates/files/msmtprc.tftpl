defaults
auth           on
tls            on
tls_starttls   on
syslog         LOG_MAIL

account        default
host           ${smtp_host}
port           ${smtp_port}
from           ${server_info_mail_from}
user           ${smtp_user}
passwordeval   cat /home/ops/.msmtp-password
