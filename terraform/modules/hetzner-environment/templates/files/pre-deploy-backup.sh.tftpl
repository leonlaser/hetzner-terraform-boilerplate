#!/bin/bash
set -euo pipefail
echo "=== Pre-deploy backup started: $(date) ==="

echo "Backing up app server..."
sudo /home/ops/scripts/borg-backup.sh
%{ if database_ip != "" }

echo "Backing up database server..."
ssh -i ~/.ssh/internal_key -o StrictHostKeyChecking=accept-new ops@${database_ip} "sudo /home/ops/scripts/borg-backup.sh"
%{ endif }

echo "=== Pre-deploy backup finished: $(date) ==="
